import React from 'react'
import ReactDOM from 'react-dom/client'
import DCDrift from './DCDrift.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DCDrift />
  </React.StrictMode>,
)
